name: Cloudflare Temporary Deploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  info:
    name: Temporary deploy disabled for automatic pushes
    runs-on: ubuntu-latest
    steps:
      - name: Explain deployment path
        run: |
          echo "Production Cloudflare deployment now runs from deploy-production.yml on every push to main."
          echo "Use this workflow only as a manual placeholder if a separate preview flow is added later."
