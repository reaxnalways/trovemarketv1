import { createSupabaseServerClient } from "@/lib/supabase/server-client";
import { bulkIncreaseServicePrices, createServicePrice, deleteServicePrice, toggleServicePrice, updateServicePrice } from "./actions";

export default async function ServicePricesPage(){const supabase=await createSupabaseServerClient();const{data}=await supabase.from("service_price_references").select("id,device_type,brand,model,fault_code,fault_label,min_price,max_price,is_active").order("device_type").order("brand").order("model").order("sort_order").order("fault_label");const rows=data??[];const deviceTypes=Array.from(new Set(rows.map(row=>String(row.device_type)).filter(Boolean)));return <main className="adminShell adminShellWide"><header className="adminTopbar"><div><h1 className="adminPageTitle">Servis Fiyat Referansları</h1><p className="adminLead">Genel, marka bazlı veya model bazlı servis fiyatlarını yönet ve yeni işlem kalemleri ekle.</p></div></header>
<section className="adminDashboardCard"><form className="adminListingForm" action={createServicePrice}><label className="adminField">Cihaz türü<select name="deviceType" required defaultValue="Telefon"><option>Telefon</option><option>Laptop</option><option>Tablet</option><option>Masaüstü Bilgisayar</option><option>Diğer</option></select></label><label className="adminField">Marka<input name="brand" placeholder="Apple — boşsa genel"/></label><label className="adminField">Model<input name="model" placeholder="iPhone 15 Pro — boşsa marka geneli"/></label><label className="adminField">Arıza / işlem<input name="faultLabel" required placeholder="Ekran değişimi"/></label><label className="adminField">Kalem kodu (opsiyonel)<input name="faultCode" placeholder="screen"/></label><label className="adminField">Minimum fiyat<input name="minPrice" inputMode="decimal" required placeholder="2500"/></label><label className="adminField">Maksimum fiyat<input name="maxPrice" inputMode="decimal" required placeholder="5000"/></label><div className="adminFormActions"><button className="adminButton" type="submit">Yeni Referans Ekle</button></div></form><p className="adminLead" style={{marginTop:16}}>Öncelik: model fiyatı → marka fiyatı → genel fiyat. Örneğin iPhone 15 Pro ekran değişimi ayrı, Apple genel ekran değişimi ayrı tanımlanabilir.</p></section>
<section className="adminDashboardCard"><h2 style={{marginTop:0}}>Toplu yüzdelik zam</h2><p className="adminLead">Tüm servis referanslarına veya seçtiğin cihaz türü/markaya aynı oranda zam uygula.</p><form className="adminListingForm" action={bulkIncreaseServicePrices}><label className="adminField">Cihaz türü<select name="deviceType" defaultValue=""><option value="">Tüm cihaz türleri</option>{deviceTypes.map(type=><option key={type} value={type}>{type}</option>)}</select></label><label className="adminField">Marka filtresi<input name="brand" placeholder="Boşsa tüm markalar"/></label><label className="adminField">Zam oranı (%)<input name="percentage" inputMode="decimal" required placeholder="10"/></label><div className="adminFormActions"><button className="adminButton" type="submit">Toplu Zammı Uygula</button></div></form></section>
<section className="listingSection"><div className="sectionHeading"><div><h2>Fiyat listesi</h2><p>{rows.length} referans</p></div></div>{rows.length?<div className="adminDraftList">{rows.map(row=><article className="adminDraftItem" key={row.id}><div><span className="productCode">{row.device_type}{row.brand?` · ${row.brand}`:" · Genel"}{row.model?` · ${row.model}`:""}</span><h3>{row.fault_label}</h3><p>{Number(row.min_price).toLocaleString("tr-TR")} ₺ – {Number(row.max_price).toLocaleString("tr-TR")} ₺ · {row.is_active?"Aktif":"Gizli"}</p></div><div className="adminInlineActions"><form action={updateServicePrice}><input type="hidden" name="id" value={row.id}/><input name="brand" defaultValue={row.brand} placeholder="Marka" aria-label="Marka"/><input name="model" defaultValue={row.model} placeholder="Model" aria-label="Model"/><input name="faultLabel" defaultValue={row.fault_label} aria-label="İşlem adı"/><input name="minPrice" defaultValue={String(row.min_price)} aria-label="Minimum fiyat"/><input name="maxPrice" defaultValue={String(row.max_price)} aria-label="Maksimum fiyat"/><button className="adminButton adminButtonSecondary" type="submit">Kaydet</button></form><form action={toggleServicePrice}><input type="hidden" name="id" value={row.id}/><input type="hidden" name="active" value={String(row.is_active)}/><button className="adminButton adminButtonSecondary" type="submit">{row.is_active?"Gizle":"Göster"}</button></form><form action={deleteServicePrice}><input type="hidden" name="id" value={row.id}/><button className="adminButton adminDangerButton" type="submit">Sil</button></form></div></article>)}</div>:<p className="emptyState">Henüz servis fiyat referansı yok.</p>}</section></main>}
